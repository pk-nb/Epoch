{div, form, button, p, h1, hr, select, option} = React.DOM

EventPanel = React.createClass
  displayName: 'EventPanel'

  render: ->
    console.log @props.selectedEvent

    div className: 'panel',
      h1 null,
        @props.selectedEvent.title
      p null,
        @props.selectedEvent.content


@.EpochUI ?= {}
@.EpochUI.EventPanel = EventPanel
